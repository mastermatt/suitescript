/**
 * SuiteScript Encryption APIs
 */

/**
 * Encrypt a string using a SHA1 hash function
 *
 * This function is available on the client side by making a server request.
 *
 * @param {string} string string to encrypt
 * @param {string} [algorithm=sha] sha1 - hash, aes - encrypt, base64 - Base 64 encode, xor - Exclusive-OR obfuscation
 * @param {string} [key] 128-bit, 192-bit, or 256-bit hex key for AES
 *
 * @return {string}
 * @since 2009.2
 */
function nlapiEncrypt(string, algorithm, key) {
}
